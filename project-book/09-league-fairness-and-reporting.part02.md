</details>

(לאורך זמן: מילוי רציף מול _C_ = 5 , _r_ = 0 _._ 8:מפלס האסימונים בדלי )14 איור אדום = דיווח שנחסם X ניקוז בהתפרצויות. נקודות ירוקות = דיווח שאושר; כשהדלי התרוקן בזמן ההתפרצות.

העקומה הכחולה היא מפלס האסימונים בדלי לאורך זמן. **מה רואים באיור:** בתעבורה תקינה הדלי מתמלא ומאפשר דיווחים )נקודות ירוקות(, אך בזמן (קצב הבקשות מרוקן את הדלי, וכל דיווח _t_ = 18 ההתפרצות המסומנת )סביב השיפוע העולה משקף את קצב המילוי **כיצד לפרש:** אדום(. Xנוסף נחסם ) ;כל ירידה חדה היא צריכת אסימון בידי דיווח; התקרה המקווקוות היא _r_ ,הדלי יתאושש מהר יותר _r_ אם נעלה את **ניתוח ״מה יקרה אם״:** . _C_ הקיבולת

---

<!-- source-pdf-page: 92 -->

;אם נקטין אתAPIוייחסמו פחות דיווחים — אך נסתכן בחריגה ממכסת ה- היא, אפוא, איזון בין תפוקה _C_ ו- _r_ ,נחנוק התפרצויות לגיטימיות. בחירת _C_ לבין בטיחות.

---

<!-- source-pdf-page: 93 -->

### דוגמה: Token Bucket rate limiter

```python
import time

class TokenBucket:
    """Simple token-bucket rate limiter for outgoing API reports."""

    def __init__(self, capacity: float, refill_rate: float):
        self.capacity = capacity          # max tokens the bucket holds
        self.refill_rate = refill_rate    # tokens added per second
        self.tokens = capacity            # start full
        self.last = time.monotonic()      # last refill timestamp

    def _refill(self) -> None:
        now = time.monotonic()
        elapsed = now - self.last
        # Continuous refill, clamped to capacity.
        self.tokens = min(
            self.capacity, self.tokens + elapsed * self.refill_rate
        )
        self.last = now

    def allow(self, cost: float = 1.0) -> bool:
        """Return True and spend a token if one is available; else block."""
        self._refill()
        if self.tokens >= cost:
            self.tokens -= cost
            return True
        return False  # caller must back off / retry later
```

---

<!-- source-pdf-page: 94 -->

### The Mandatory Signed Report חתום ומחייב JSON מבנה הדיווח: 9.3.3
אחיד ומחייב, ונשלח JSON דיווח המשחק אינו טקסט חופשי. הוא נארז במבנה מכיל את כל פרטי הזהות של הקבוצה, JSONכקובץ מצורף להודעת הדואר. ה- ,הצהרות חומרה חתומותFastMCPשלה, כתובות שרתי ה- GitHubכתובות ה- SHA קריפטוגרפית, חותמת-הזמן של המשחק, ואישורי הסכמה-הדדית מגובי (,מובילplaintext.כל ניסיון לשלוח דיווח פתוח, שאינו קריא-מכונה )256 לדחיית הדיווח.

### חובה: הסכמה על התוצאה ושני דוחות נפרדים
**כל קבוצה חייבת** , ו **שתי הקבוצות חייבות להסכים על תוצאתו** בתום המשחק את דוח סיום המשחק אל המרצה — בנפרד ובפורמט **לשלוח בעצמה אם לא יתקבל דוח מאחד** המחייב. הדיווח אינו באחריות צד אחד בלבד. — גם אם ניצח על הלוח. **הצדדים, אותו צד לא יזוכה בניקוד עבור המשחק** הדוחות הנפרדים הם התנאי לכך ששתי _שני_ הסכמה על התוצאה ומשלוח הקבוצות יזוכו בניקוד המגיע להן.

מצורף, ודוגמה מלאה שלו מצורפת לספר JSON הדוח כולו נשלח כקובץ **JSON ארבעה קובצי** למעשה מצורפים לספר . **]** קובץ התוצאות **[** זה כקובץ , המכסים את מחזור-חייו המלא של המשחק; שם-המשתנה של כל אחד **לדוגמה**

: ומהם מוגדר בטבלת המשתנים שבנספח

מרכזת את כל הנתונים **הצהרת טרם-משחק.** — **]** קובץ ההצהרה **[** - זהות שתי הקבוצות של המשחק כולו )כלל תת-המשחקים(: _קבועים_ ה ,מפרטיMCPוחבריהן, כתובות מאגרי השוטר והגנב, כתובות שרתי ה- החומרה, מודל השפה, תקרת הטוקנים המוסכמת, וזמני תחילת המשחק לקבע, חתום קריפטוגרפית, את כל שאינו משתנה תפקידה: וסיומו. במהלך המשחק.

כל הפרמטרים הכמותיים של **התצורה המוסכמת.** — **]** קובץ התצורה **[** - תפקידו: (, נעולים קריפטוגרפית וזהים בין הצדדים. והמשחקון )נספח להגדיר את חוקי הפיזיקה והניקוד שעליהם הסכימו שתי הקבוצות. רשומת צעד-אחר-צעד: התחייבויות **יומן המשחקון.** — **]** קובץ היומן **[** - ,מהלכים, רמזים ושדות הדיון עם מודל השפה, לצדCommitReveal .תפקידו: לאפשר אימות קריפטוגרפי מלא בסימולטורhashוהNonceה-

---

<!-- source-pdf-page: 95 -->

(.7 השחזור )פרק סיכום כלל תת-המשחקים: **דוח התוצאות הסופי.** — **]** קובץ התוצאות **[** - ניקוד כל קבוצה בכל משחקון והתוצאה המצטברת, לשקלול ציון הליגה כתובת דיווחי **[** זהו הדוח המחייב הנשלח בדוא״ל אל בידי המרצה. . **]** הסוכן

(,ושם כל קובץ נגזר ממזההgame_uidארבעת הקבצים נושאים מזהה משותף ) —כך שלעולם לא יתערבבו קבצים ממשחקים שונים. (game_idהמשחק ) הקבוצות, את _שתי_ של GitHubהשדות המחייבים בדוח כוללים את קישורי ה- (,ואת סך הטוקנים שנצרכו.5 מזהה הקומיט של כל משחקון )פרק

### ודיווח קריא-מכונה Rate Limit 429 כללי ברזל:
HTTP מוחזרת כשגיאת Google של API חריגה ממכסת ה- **מכסה וקצב.** .שגיאה זו אינה תקלה חולפת — התעקשות429 (Too Many Requests) עיוורת ושליחה חוזרת מיד לאחריה עלולה להוביל להשעיית החשבון בידי (,ולהמתין לחלון-הזמן הבא.back off,לסגת )429הספק. יש לכבד את ה- מובנה, אחיד וקריא-מכונה, JSON דיווח חייב להיות **פורמט הדיווח.** כל ניסיון לשלוח דיווח פתוח בטקסט חופשי הנשלח כקובץ מצורף. — ומשמעות _דחיית הדיווח_ (,שאינו ניתן לניתוח אוטומטי, יוביל לplaintext) הדחייה עלולה להיות אובדן נקודות הליגה של אותו סבב.

.הגישהAPIשכבת האבטחה של הדיווח נשענת גם על הרשאה מבוקרת אל ה- אינה מסתמכת על סיסמה גולמית אלא על אסימוני Gmail של הסוכן אל ,המאפשר הענקת הרשאה מצומצמת[34] OAuth 2.0 הרשאה מדודים לפי תקן ושל אישורי OAuth (וביטולה בעת הצורך. תהליך ההגדרה המלא שלscoped) . אמפורט בנספח Gmail APIה-

### GitHub Sub :מבנה, תוכן ושני מאגריםGitHubהגשה ב- 9.4 mission: Structure, Contents, Two Repositories
_ציבורי_ : או שהוא **כל מאגר חייב להיות נגיש למרצה** .GitHubההגשה מתבצעת ב- **כל** . **]** כתובת המרצה **[** במפורש עם כתובת המרצה _משותף_ (,או שהואpublic) : מאגר אחד לסוכן השוטר ומאגר שני לסוכן **קבוצה מגישה שני מאגרים נפרדים** — קישור למאגר השוטר וקישור למאגר הגנב. **שני קישורים** הגנב, ומספקת

---

<!-- source-pdf-page: 96 -->

### שני מאגרים, קישור צולב חובה, ושני קישורים בהגשה
GitHub סוכנים — שוטר וגנב — בשני מאגרי _שני_ כל קבוצה מפתחת (. **]** כתובת המרצה **[** , הנגישים למרצה )ציבוריים או משותפים עם _נפרדים_ של כל מאגר יכלול את הקישור אל המאגר README.md שקובץ ה- **חובה** של השוטר מפנה אל מאגר הגנב, README של אותה קבוצה: ה- _השני_ הקישורים )שוטר וגנב(, _שני_ חייב להכיל את _מוֹדל_ ולהפך. הקובץ המוגש ב _ארבעת_ המצורף — מופיעים JSONובדוא״ל סיום המשחק — בקובץ ה- .B ושני הקישורים של קבוצה A הקישורים: שני הקישורים של קבוצה

### Mandatory Repository Contents תוכן החובה של המאגר 9.4.1
)הדוח האקדמי, README.md חייב לכלול, לכל הפחות: קובץ GitHub כל מאגר (ששימשוPRD(;קובצי הגדרת המוצר )config/ראו להלן(; קובצי התצורה ) (.קבציםTODO(;וקובצי המשימות )PLANלבניית הקוד; קובץ תוכנית העבודה ) אלה מספרים את סיפור הפיתוח ומאפשרים לבוחן לשחזר את דרך העבודה — לא רק את התוצאה הסופית. מדריך ההגשה המלא — ענפים, תיוג גרסה . גורשימת בדיקה — מרוכז בנספח

---

<!-- source-pdf-page: 97 -->

**README Contents READMEתוכן קובץ ה- 9.4.2** לב ההגשה התיעודית הוא הדוח האקדמי שבקובץ גורע מן ההגשה:

שבשורש כל README.md לב ההגשה התיעודית הוא הדוח האקדמי שבקובץ מאגר. הרשימה שלהלן מפרטת את מרכיבי החובה — היעדר כל אחד מהם

**READMEתוכן החובה של הדוח האקדמי ב-**

תיאור מדעי של הפורמליזם **הנבחר. DecPOMDPמודל ה-** 1. שאימצתם למידול המרוץ — מרחב המצבים, התצפיות, ואי.1 הוודאות — כפי שנפרש בפרק פרק דיון בהתלבטויות הפיתוח סביב **.FastMCP דילמות התזמור של** 2. תזמור התקשורת בין הסוכנים: ניהול תורות, טיפול בכשלי רשת, (.8 ,פרק2 )פרק OrchestratorוהGatekeeperותפקידי ה-

פירוט מנגנון קבלת ההחלטות שבחרתם **האסטרטגיות שמומשו.** 3. — בייסיאנית(, אמונות מפת מנהטן, )מרחק היוריסטיקות (,או, כאפשרות אופציונלית,LLMאסטרטגיה מבוססת מודל שפה )

(.6 )פרק QLearning אם אימנתם סוכן **).RLעקומות למידה (אם נעשה שימוש ב-** .4 בלמידת חיזוקים — עקומות הלמידה כראיה אמפירית להתכנסות

המדיניות.

)מפת האמונות( Live GUI מן ה- **צילומי מסך — חובה מוחלטת.** 5. (.7 )פרק Verified OK (המדגיםReplay Appומיישום השִחזור ) השני של GitHubהקישור אל מאגר ה- **קישור למאגר הנלווה.** .6 הקבוצה )שוטר/גנב(, כמתחייב לעיל.

### סיכום הפרק 9.5
ראינו שהפרויקט נבחן לא במעבדה סגורה אלא בליגה חיה, שבה תמריץ הגיוון מתגמל התמודדות מול יריבים חדשים וההוגנות החישובית מתגמלת חוכמה אלגוריתמית על פני כוח חישוב גולמי. פירקנו את אוטומציית הדיווח —מנהל המכסה, דלי-האסימונים Gatekeeperואת תבנית ה- Gmail API מעל —כשכבת-הגנה שבלעדיה סוכן אוטונומי עלול להמיט אסון על DOSוגלאי ה- JSONחשבון חי. עמדנו על הכלל המתמטי של דלי-האסימונים ועל מבנה ה-

---

<!-- source-pdf-page: 98 -->

החתום והמחייב. בפרק הבא נרחיב את היריעה אל שיקולי הפיתוח הכוללים של המערכת — עמידות, בדיקות ופריסה.

---

[← Chapter 8 — Agent Architecture and Reliability](08-agent-architecture-and-reliability.md) · [Contents](README.md) · [Chapter 10 — Development Priorities and Process →](10-development-priorities.md)
